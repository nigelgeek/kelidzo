/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Output the production build to a `dist` directory for Vercel.
  distDir: 'dist',
}

export default nextConfig